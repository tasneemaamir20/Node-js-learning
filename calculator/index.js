const { calculateSum } = require("./Sum");
const { calculateMultiply } = require("./Multiply");
const { subtract } = require("./subtract");
const { divide } = require("./divide");

module.exports = { calculateMultiply, calculateSum, subtract, divide };
